// `ALPHA_CHECK` shouldn't really be neccessary, I don't think, but it seems to work around some Iris issue.
#define ALPHA_CHECK
#define NO_NORMAL
#define TRANSLUCENT
#include "/prog/lit_forward.fsh"
