#version 120

/* gbuffers_clouds.fsh — Descarta las nubes vanilla (las nuestras van en
   composite.fsh). Si algún día prefieres las nubes vanilla, borra estos
   dos archivos (gbuffers_clouds.vsh/.fsh). */

void main() {
    discard;
}
